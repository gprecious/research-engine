# superpowers brainstorm session files
.superpowers/

# research outputs (acceptance runs and real usage both land here)
research/

# llm-wiki content (개인 지식 — 플러그인 배포 제외, 발행은 wiki/ 자체 git repo로)
# repo 루트의 콘텐츠 vault 만 무시 — lib/wiki/ 소스는 ship 되어야 하므로 leading-slash 로 앵커
/wiki/

# Node / Python / editors
node_modules/
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
.idea/
.vscode/

# bench raw outputs are reproducible — only design + harness + findings are committed.
bench/runs/

# local Claude Code workspace + Playwright MCP scratch
.claude/
.playwright-mcp/

# ad-hoc UI-verification screenshots saved at repo root
/*.png

# research-design
.env.research-design
test-results/
playwright-report/
research/*/design/runs/
research/*/design/handoff/
research/*/design/app/
# allow hand-authored contract files
!research/*/design/scenarios.json

# Evolve loop: commit ledger, ignore ephemeral candidates
!research/_index/evolve-ledger.json
agents/*.candidate.md
